# Prompt: interrogate before building

> Before changing the product, interrogate the goal. Identify the user, the decision they are trying to make, the most important success measure, and anything you would otherwise have to guess. Then propose a small implementation plan with explicit non-goals, system boundaries, acceptance criteria, invariants, and a validation path. Stop for my approval before implementation.

The presenter can answer with:

> One junior developer, using it locally. The real pain is losing momentum across several applications. Success means the app can answer “What should I do next?” without turning into a full recruitment platform.
